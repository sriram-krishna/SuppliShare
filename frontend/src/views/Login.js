import React from 'react';

const Login = () => {
  const handleLogin = () => {
    
  };

  return (
    <div>
      <button onClick={handleLogin}>Login</button>
    </div>
  );
};

export default Login;
